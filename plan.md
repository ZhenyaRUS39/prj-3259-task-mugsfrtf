# Plan: prj-3259-task-mugsfrtf

**Project:** `PRJ-9017`  
**Task ID:** `task-mugsfrtf`  
**Repo:** `prj-3259-task-mugsfrtf`  

## Summary

План: создать Python CLI (markdown2pdf) с argparse, конвертацией через WeasyPrint, тестами на pytest и README. Разбито на 4 последовательных subtask.

## Subtasks

### 1. Инициализировать структуру репозитория

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать pyproject.toml с зависимостями (markdown, weasyprint, click/argparse), README.md, .gitignore, базовую структуру каталогов src/ и tests/. Настроить entry point для CLI.
- **Test plan:** pip install -e . проходит без ошибок; CLI команда markdown2pdf --help выводит usage

### 2. Реализовать ядро конвертации markdown→HTML→PDF

- **ID:** `sub-2`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Написать модуль src/markdown2pdf/converter.py: функция convert(md_path, pdf_path, css_path=None) — читает MD, конвертирует в HTML через markdown, применяет CSS и рендерит PDF через weasyprint.
- **Test plan:** convert() корректно обрабатывает заголовки, списки, код, ссылки; PDF создаётся и валиден (размер > 0, magic bytes %PDF)
- **Dependencies:** `sub-1`

### 3. Реализовать CLI-обёртку

- **ID:** `sub-3`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Добавить src/markdown2pdf/cli.py с argparse: позиционный аргумент input, флаги -o/--output, --css, --title, --encoding. Зарегистрировать entry point в pyproject.toml. Обработка ошибок с понятными сообщениями.
- **Test plan:** markdown2pdf README.md -o out.pdf создаёт валидный PDF; --css custom.css применяет стили; несуществующий файл даёт ошибку с exit code != 0
- **Dependencies:** `sub-2`

### 4. Написать тесты и финальную документацию

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** tests/test_converter.py: unit-тесты на convert() (заголовки, списки, таблицы, код), CLI-тесты через subprocess. Дополнить README примерами использования, секцией Troubleshooting, бейджем CI.
- **Test plan:** pytest tests/ проходит 100%; coverage converter.py >= 80%; README содержит quickstart, примеры и troubleshooting
- **Dependencies:** `sub-3`


---
*Generated by Hermes Orchestrator*
